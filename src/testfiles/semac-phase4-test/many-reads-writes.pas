program funcTest(input, output);
var
        i, j, k, l, m:integer;
        n, o, p, q:real;

begin
        read(i,j,l);
        read(m);
        read(n,o,p,q);
        write(i,j,l,m);
        write(j,l);
        write(p,q);
        write(m,l,i)
end.